$$
x(t+1)=f(x(t))+g(x(t))\left[u\left(r(t)-\hat{x}(t)+e^*(t)\right)+d_e(t)\right]
$$


$$
r(t+1)=f(r(t))+g(r(t))\left[u\left(e^*(t)\right)+g^{-1}(r(t)) g(\hat{x}(t)) \hat{d}(t)\right]
$$


$$
\hat{x}(t+1)=f(\hat{x}(t))+g(\hat{x}(t)) u\left(r(t)-\hat{x}(t)+e^*(t)\right)+K_o(x(t)-\hat{x}(t))
$$


扰动估计 $\hat{d}(t)=g^{-1}(\hat{x}(t)) K_o(x(t)-\hat{x}(t))$ ，即存在

$$
g(\hat{x}(t)) \hat{d}(t)=K_o(x(t)-\hat{x}(t))
$$

注意上面的等式两边是向量。



$\begin{aligned} & r(t+1)=f(r(t))+g(r(t))\left[u\left(e^*(t)\right)+g^{-1}(r(t)) g(\hat{x}(t)) \hat{d}(t)\right] \\ & =f(r(t))+g(r(t)) u\left(e^*(t)\right)+g(r(t)) g^{-1}(r(t)) g(\hat{x}(t)) \hat{d}(t) \\ & =f(r(t))+g(r(t))\left[u\left(e^*(t)\right)+g^{-1}(r(t)) K_o(x(t)-\hat{x}(t))\right]\end{aligned}$



因此，如果要将扰动估计注入逆模型输入端，关键是估计 $g^{-1}(r(t))$

当前单关节离散模型下的输入逆：
当前 C++ 控制器使用的正模型为半隐式欧拉单关节模型：

$$
\dot q_{k+1}=\dot q_k+\frac{T_s}{J_\mathrm{eff}}(u_k-\beta_k)
$$

$$
q_{k+1}=q_k+T_s\dot q_{k+1}
$$

其中 $\beta_k=b\dot q_k+A\sin q_k+B\cos q_k+\tau_0$ 属于漂移项 $f(x_k)$。因此输入矩阵为

$$
g=
\begin{bmatrix}
T_s^2/J_\mathrm{eff}\\
T_s/J_\mathrm{eff}
\end{bmatrix}
$$

由于这里 $g$ 是 $2\times1$ 列向量，应使用 Moore-Penrose 左伪逆：

$$
g^+=(g^Tg)^{-1}g^T
$$

于是输入端等效扰动可先按下式离线估计：

$$
\hat d_u=g^+K_o(x-\hat x)
$$

若需要抑制速度通道噪声放大，可以使用加权伪逆：

$$
g_W^+=(g^TWg)^{-1}g^TW
$$

当前 P4 髋膝配置中 $T_s=0.002$。RightHipPitch 的 $J_\mathrm{eff}=1.00508532$，因此

$$
g=
\begin{bmatrix}
3.9798\times10^{-6}\\
1.9899\times10^{-3}
\end{bmatrix},
\qquad
g^+\approx
\begin{bmatrix}
1.0051 & 502.5406
\end{bmatrix}
$$

RightKnee 的 $J_\mathrm{eff}=0.2501484$，因此

$$
g=
\begin{bmatrix}
1.5991\times10^{-5}\\
7.9953\times10^{-3}
\end{bmatrix},
\qquad
g^+\approx
\begin{bmatrix}
0.2501 & 125.0737
\end{bmatrix}
$$

这些数值只用于离线分析，不应直接替换当前实时控制器中的 `ku_q` 和 `ku_dq`。原因是 $g^+$ 的速度通道系数明显大于当前经验补偿增益，直接接入会放大速度测量噪声和观测器误差。



求解 $g^{-1}(r(t))$ 思路：
注意到离散模型形式：
$$
r(t+1)=f(r(t))+g(r(t)) u(\tilde{e}(t)), \text { 其中 } u(\tilde{e}(t))=u\left(e^*(t)\right)+g^{-1}(r(t)) K_o(x(t)-\hat{x}(t)) \text { 。 }
$$

因此，$g(r(t))=[r(t+1)-f(r(t))] u^{-1}(\tilde{e}(t))$
显然，采用 NN 做对象模型（正模型）估计器时，如果控制输入为 0 ，NN 模型输出就是漂移阵 $f(\cdot)$ 的估计 $\hat{f}(\cdot)$ 。



求解 $g^{-1}(r(t))$ 步骤：
Step1：以 $\langle r(t), 0\rangle$ 为正模型输入，获得漂移阵估计 $\hat{f}(r(t))$ ；
Step2：以 $\langle r(t+1), r(t)\rangle$ 为逆模型输入，获得控制输入估计 $u(\tilde{e}(t))$ ；
Step3：求 $g(r(t))$ 估计：$g(r(t))=[r(t+1)-\hat{f}(r(t))] u^{-1}(\tilde{e}(t))$
Step4：求 $g(r(t))$ 的右伪逆 $g^{-1}(r(t))$ 。
注意：由于正逆模型有建模误差，会产生虚拟的输入误差，这个偏差来自闭环外，无法抑制。

