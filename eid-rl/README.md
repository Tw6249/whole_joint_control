# EID-RL Raw Materials

本目录保存 EID-RL 相关的早期原始材料，包括截图、临时图片和 `eid_rl5.23.md` 原始记录。

这些内容主要用于溯源，不作为最终报告入口。整理后的正式报告位于：

- `eid_rl_report_package/`

专题复现分析位于：

- `analysis_artifacts/eid_joint_coupling/`
- `analysis_artifacts/right_knee_interpolation/`
