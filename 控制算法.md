# 控制算法说明

本文只描述当前控制器的核心算法递推关系。为突出纯粹控制律，
不展开工程保护、命令下发、实验入口或其他控制变体。

## 1. 状态与参考量

单个关节的状态定义为

$$
x_k =
\begin{bmatrix}
q_k \\
\dot q_k
\end{bmatrix}
$$

其中 $q_k$ 为关节位置，$\dot q_k$ 为关节速度。
控制输入 $u_k$ 为该关节的标量力矩。

每个控制周期给定当前参考状态和下一步参考状态：

$$
r_k =
\begin{bmatrix}
q_k^* \\
\dot q_k^*
\end{bmatrix},
\qquad
r_{k+1} =
\begin{bmatrix}
q_{k+1}^* \\
\dot q_{k+1}^*
\end{bmatrix}
$$

参考增量为

$$
\Delta r_k=r_{k+1}-r_k
$$

即

$$
\Delta q_k=q_{k+1}^*-q_k^*
$$

$$
\Delta \dot q_k=\dot q_{k+1}^*-\dot q_k^*
$$

### 1.1 Preview-MPC 参考生成

当策略层只以较低频率给出离散关节位置参考时，控制器可先使用
Preview-MPC 生成每个控制周期使用的平滑参考。Preview-MPC 属于参考生成层，
不改变后续 PD、逆模型或 EID 控制律。

策略参考采样周期记为

$$
T_p
$$

控制周期记为

$$
T_s
$$

单个策略周期内的控制步数为

$$
N_p=\mathrm{round}\left(\frac{T_p}{T_s}\right)
$$

若使用 $n$ 个预览点，则优化窗口长度为

$$
N_h=nN_p
$$

当前实现中 $n$ 被限制在 $1$ 到 $3$ 之间。实验中的 `preview2`
对应

$$
n=2
$$

也就是使用第一个策略点作为硬目标，并使用第二个策略点作为软预览目标。

Preview-MPC 对每个关节独立求解。其运动学状态定义为

$$
z_i=
\begin{bmatrix}
q_i \\
\dot q_i \\
\ddot q_i
\end{bmatrix}
$$

优化变量为控制窗口内的 jerk 序列

$$
J=
\begin{bmatrix}
j_0 & j_1 & \cdots & j_{N_h-1}
\end{bmatrix}^{T}
$$

其中

$$
j_i=\dddot q_i
$$

在每个控制步内假设 jerk 为常值，则离散运动学递推为

$$
\ddot q_{i+1}
=
\ddot q_i+T_s j_i
$$

$$
\dot q_{i+1}
=
\dot q_i
+T_s\ddot q_i
+\frac{1}{2}T_s^2j_i
$$

$$
q_{i+1}
=
q_i
+T_s\dot q_i
+\frac{1}{2}T_s^2\ddot q_i
+\frac{1}{6}T_s^3j_i
$$

设预览到的策略位置点为

$$
q_1^{p},q_2^{p},\ldots,q_n^{p}
$$

其中 $q_1^{p}$ 是当前策略周期必须命中的第一个目标点，
$q_2^{p},\ldots,q_n^{p}$ 是后续预览点。Preview-MPC 的约束为

$$
q_{N_p}=q_1^{p}
$$

后续预览点不作为硬约束，而是进入代价函数：

$$
q_{\ell N_p}\approx q_\ell^{p},
\qquad
\ell=2,\ldots,n
$$

因此可将优化问题写为

$$
\min_{J}\ L_\mathrm{mpc}
$$

其中

$$
\begin{aligned}
L_\mathrm{mpc}
=&
\sum_{i=0}^{N_h-1}
\left(
w_j j_i^2
+w_v \dot q_i^2
+w_a \ddot q_i^2
\right) \\
&+
\sum_{\ell=2}^{n}
w_p
\left(
q_{\ell N_p}-q_\ell^{p}
\right)^2 \\
&+
w_{Tv}\dot q_{N_h}^2
+w_{Ta}\ddot q_{N_h}^2
\end{aligned}
$$

并满足硬约束

$$
q_{N_p}=q_1^{p}
$$

求解后只执行第一个策略周期内的参考：

$$
i=0,1,\ldots,N_p
$$

输出给底层控制器的参考为

$$
r_k=
\begin{bmatrix}
q_k^* \\
\dot q_k^*
\end{bmatrix}
$$

其中 $q_k^*$ 和 $\dot q_k^*$ 来自 Preview-MPC 生成的平滑轨迹；
$\ddot q_k^*$ 同时可用于记录和评估参考加速度。下一策略周期到来时，
控制器重新读取新的预览点并再次求解。

相对于零速五次样条插值，Preview-MPC 的作用是利用未来策略点提前分配
速度、加速度和 jerk，从而降低参考加速度尖峰、参考 jerk 以及由参考突变
引起的力矩变化量。

## 2. 标称单关节模型

控制器内部采用二阶单关节标称模型：

$$
J_\mathrm{eff}\ddot q
=
u
- b\dot q
- A\sin q
- B\cos q
- \tau_0
$$

其中 $J_\mathrm{eff}$ 为等效惯量，$b$ 为阻尼系数，
$A\sin q+B\cos q$ 为重力相关项，$\tau_0$ 为常值偏置。

离散更新采用半隐式欧拉形式：

$$
\ddot q_k =
\frac{
u_k-b\dot q_k-A\sin q_k-B\cos q_k-\tau_0
}{
J_\mathrm{eff}
}
$$

$$
\dot q_{k+1}=\dot q_k+T_s\ddot q_k
$$

$$
q_{k+1}=q_k+T_s\dot q_{k+1}
$$

记该标称正向模型为

$$
x_{k+1}=\hat\Phi(x_k,u_k)
$$

## 3. 中心反馈状态与 EID 估计

控制器维护标称预测状态

$$
\hat x_k =
\begin{bmatrix}
\hat q_k \\
\hat{\dot q}_k
\end{bmatrix}
$$

以及 EID 估计量

$$
\eta_k =
\begin{bmatrix}
\eta_{q,k} \\
\eta_{\dot q,k}
\end{bmatrix}
$$

中心反馈状态定义为

$$
\bar x_k=\hat x_k+\eta_k
$$

后续反馈控制不直接围绕测量状态 $x_k$ 构造，而是围绕中心反馈状态
$\bar x_k$ 构造。

## 4. 解析逆模型

逆模型基于参考状态 $r_k$ 和参考增量 $\Delta r_k$，
计算一步跟踪所需的标称前馈力矩 $u_k^*$。

目标下一步状态为

$$
q_{k+1}^{\mathrm{tar}}=q_k^*+\Delta q_k
$$

$$
\dot q_{k+1}^{\mathrm{tar}}
=
\dot q_k^*+\Delta \dot q_k
$$

定义参考点处的模型偏置项：

$$
\beta_k =
b\dot q_k^*
+A\sin q_k^*
+B\cos q_k^*
+\tau_0
$$

由位置目标反解得到的力矩为

$$
\tau_{q,k}
=
\beta_k
+J_\mathrm{eff}
\frac{
q_{k+1}^{\mathrm{tar}}-q_k^*-T_s\dot q_k^*
}{
T_s^2
}
$$

由速度目标反解得到的力矩为

$$
\tau_{\dot q,k}
=
\beta_k
+J_\mathrm{eff}
\frac{
\dot q_{k+1}^{\mathrm{tar}}-\dot q_k^*
}{
T_s
}
$$

令

$$
a_q=\frac{T_s^2}{J_\mathrm{eff}},
\qquad
a_{\dot q}=\frac{T_s}{J_\mathrm{eff}}
$$

则解析逆模型使用加权融合：

$$
u_k^*
=
\frac{
w_q a_q^2\tau_{q,k}
+w_{\dot q}a_{\dot q}^2\tau_{\dot q,k}
}{
w_q a_q^2
+w_{\dot q}a_{\dot q}^2
}
$$

其中默认权重为

$$
w_q=\frac{0.5}{T_s^2},
\qquad
w_{\dot q}=1
$$

如果配置中给定了正的逆模型权重，则使用配置权重替代默认权重。

逆模型残差可写为

$$
\rho_k=
\hat\Phi(r_k,u_k^*)-(r_k+\Delta r_k)
$$

该残差表示前馈力矩在标称模型下一步预测结果与参考目标之间的偏差。

## 5. EID 输入补偿

EID 估计量先被映射为输入力矩补偿：

$$
\eta_{u,k}
=
k_{u,q}\eta_{q,k}
+k_{u,\dot q}\eta_{\dot q,k}
$$

前馈力矩修正为

$$
u_{k,\mathrm{ff}}
=
u_k^*-\eta_{u,k}
$$

其中 $k_{u,q}$ 和 $k_{u,\dot q}$ 为输入补偿增益。

## 6. 前馈力矩到等效目标状态

实际反馈增益写成

$$
K=
\begin{bmatrix}
k_p & k_d
\end{bmatrix}
$$

由于控制输入为标量力矩，代码用 $K$ 的伪逆把前馈力矩转成
等效状态偏移：

$$
K^\dagger
=
\frac{1}{k_p^2+k_d^2}
\begin{bmatrix}
k_p \\
k_d
\end{bmatrix}
$$

于是等效目标状态为

$$
r_{d,k}=r_k+K^\dagger u_{k,\mathrm{ff}}
$$

展开为

$$
q_{d,k}
=
q_k^*
+\frac{k_p}{k_p^2+k_d^2}u_{k,\mathrm{ff}}
$$

$$
\dot q_{d,k}
=
\dot q_k^*
+\frac{k_d}{k_p^2+k_d^2}u_{k,\mathrm{ff}}
$$

## 7. 核心控制律

反馈误差定义为

$$
e_k=r_{d,k}-\bar x_k
$$

即

$$
e_{q,k}=q_{d,k}-\bar q_k
$$

$$
e_{\dot q,k}=\dot q_{d,k}-\bar{\dot q}_k
$$

控制力矩为

$$
u_k
=
k_p e_{q,k}
+k_d e_{\dot q,k}
$$

将 $r_{d,k}=r_k+K^\dagger u_{k,\mathrm{ff}}$ 代入，可得等价形式：

$$
u_k
=
u_{k,\mathrm{ff}}
+K(r_k-\bar x_k)
$$

再代入 $u_{k,\mathrm{ff}}=u_k^*-\eta_{u,k}$，得到当前算法的核心控制律：

$$
u_k
=
u_k^*
-\eta_{u,k}
+K(r_k-\bar x_k)
$$

也就是

$$
u_k
=
u_k^*
-k_{u,q}\eta_{q,k}
-k_{u,\dot q}\eta_{\dot q,k}
+k_p(q_k^*-\bar q_k)
+k_d(\dot q_k^*-\bar{\dot q}_k)
$$

该式表明，最终力矩由三部分组成：

- 解析逆模型给出的标称前馈力矩 $u_k^*$；
- EID 估计量映射到输入端后的抵消项 $-\eta_{u,k}$；
- 围绕中心反馈状态 $\bar x_k$ 的 PD 修正项。

## 8. 观测器与 EID 更新

控制力矩计算完成后，标称观测器按中心反馈状态推进：

$$
\hat x_{k+1}
=
\hat\Phi(\bar x_k,u_k)
$$

当前算法中的 EID 更新误差为

$$
\tilde x_k=x_k-\bar x_k
$$

而不是 $x_k-\hat x_k$。

分量形式为

$$
\tilde q_k=q_k-\bar q_k
$$

$$
\tilde{\dot q}_k=\dot q_k-\bar{\dot q}_k
$$

EID 更新律为

$$
\eta_{q,k+1}
=
\alpha k_{o,q}\tilde q_k
+(1-\alpha)\eta_{q,k}
$$

$$
\eta_{\dot q,k+1}
=
\alpha k_{o,\dot q}\tilde{\dot q}_k
+(1-\alpha)\eta_{\dot q,k}
$$

矩阵形式为

$$
\eta_{k+1}
=
\alpha K_o(x_k-\bar x_k)
+(1-\alpha)\eta_k
$$

其中

$$
K_o=
\begin{bmatrix}
k_{o,q} & 0 \\
0 & k_{o,\dot q}
\end{bmatrix}
$$

下一周期的中心反馈状态为

$$
\bar x_{k+1}=\hat x_{k+1}+\eta_{k+1}
$$


## 9. 单周期算法流程

每个控制周期执行以下递推：

1. 读取当前测量状态 $x_k=[q_k,\dot q_k]^T$。
2. 取得参考状态 $r_k$ 和下一步参考状态 $r_{k+1}$。
3. 计算参考增量 $\Delta r_k=r_{k+1}-r_k$。
4. 由 $\hat x_k$ 和 $\eta_k$ 构造中心反馈状态
   $\bar x_k=\hat x_k+\eta_k$。
5. 用解析逆模型计算 $u_k^*$。
6. 计算 EID 输入补偿 $\eta_{u,k}$。
7. 计算前馈修正力矩 $u_{k,\mathrm{ff}}=u_k^*-\eta_{u,k}$。
8. 通过 $K^\dagger$ 构造等效目标状态 $r_{d,k}$。
9. 计算反馈误差 $e_k=r_{d,k}-\bar x_k$。
10. 计算控制力矩 $u_k=k_p e_{q,k}+k_d e_{\dot q,k}$。
11. 用 $\hat\Phi(\bar x_k,u_k)$ 更新 $\hat x_{k+1}$。
12. 用 $x_k-\bar x_k$ 更新 $\eta_{k+1}$。
13. 构造下一周期使用的 $\bar x_{k+1}$。

## 10. 核心算法摘要

当前控制算法可以压缩为以下递推组：

$$
\bar x_k=\hat x_k+\eta_k
$$

$$
u_k^*=\hat{\mathcal I}(r_k,r_{k+1}-r_k)
$$

$$
\eta_{u,k}=
\begin{bmatrix}
k_{u,q} & k_{u,\dot q}
\end{bmatrix}
\eta_k
$$

$$
u_k=u_k^*-\eta_{u,k}+K(r_k-\bar x_k)
$$

$$
\hat x_{k+1}=\hat\Phi(\bar x_k,u_k)
$$

$$
\eta_{k+1}
=
\alpha K_o(x_k-\bar x_k)
+(1-\alpha)\eta_k
$$

$$
\bar x_{k+1}=\hat x_{k+1}+\eta_{k+1}
$$




## 11. 参考域补偿思路

上面的当前算法把 EID 估计量映射到输入端，形成
$-\eta_{u,k}$ 力矩补偿项。另一种思路是把 EID 估计量用于修正下一步参考，
也就是在参考域中提前抵消估计偏差。

参考域补偿先定义修正后的下一步参考：

$$
r_{k+1}^{c}=r_{k+1}-\eta_k
$$

于是逆模型使用的参考增量变为

$$
\Delta r_k^c
=
r_{k+1}^{c}-r_k
=
r_{k+1}-\eta_k-r_k
$$

前馈力矩由修正参考增量生成：

$$
u_k^*
=
\hat{\mathcal I}(r_k,\Delta r_k^c)
$$

此时不再显式构造输入域补偿项 $\eta_{u,k}$，而是直接使用
$u_k^*$ 作为前馈力矩。等效目标状态为

$$
r_{d,k}=r_k+K^\dagger u_k^*
$$

反馈误差仍围绕中心反馈状态构造：

$$
e_k=r_{d,k}-\bar x_k
$$

因此参考域补偿下的控制律可写为

$$
u_k
=
u_k^*
+K(r_k-\bar x_k)
$$

其中 $u_k^*$ 已经包含了由 $r_{k+1}-\eta_k$ 引入的参考修正影响。

从结构上看，两种思路的差别是：

- 输入域补偿：先按原始参考求 $u_k^*$，再在力矩端减去 $\eta_{u,k}$；
- 参考域补偿：先用 $\eta_k$ 修正下一步参考，再由逆模型重新生成 $u_k^*$。

两者的观测器更新和 EID 更新仍保持同一形式：

$$
\hat x_{k+1}=\hat\Phi(\bar x_k,u_k)
$$

$$
\eta_{k+1}
=
\alpha K_o(x_k-\bar x_k)
+(1-\alpha)\eta_k
$$
