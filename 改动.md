报告不用大改数据和图，**主要改写“口径、结构、结论和后续计划”**。我建议按下面方式改：**EID 明确写成“包含解析逆模型的整体结构”；结果按证据强弱分层；限制前置；下一步计划从泛泛而谈改成可执行实验清单。**

---

# 一、全文统一改法

全文第一次出现 EID 时，统一写成：

> **包含解析逆模型的输入域 EID 整体结构**

后文可以简称：

> **EID 整体结构**
> 或
> **输入域 EID 整体结构**

不要再写成容易被理解为“单独 EID 模块”的说法，比如：

> EID 单独带来……
> EID 通道独立证明了……
> 解析逆模型前馈和 EID 的贡献分别是……

因为你们现在的算法定义里，最终控制律本来就是：

[
u_k=u_k^*-\eta_{u,k}+K(r_k-\bar x_k)
]

其中 (u_k^*) 来自解析逆模型，(-\eta_{u,k}) 来自输入端补偿，反馈围绕中心反馈状态 (\bar x_k) 构造；这三者共同构成当前 EID 整体结构。

---

# 二、摘要直接替换

现在摘要信息很全，但略像“实验流水账”。建议直接替换为下面版本。

## 建议摘要

> 本文报告 Unitree H1 右髋俯仰与右膝关节在悬挂或机械保护条件下的第一轮实物实验结果。实验对象为**包含解析逆模型的输入域 EID 整体结构**，并与电机位置模式 PD 基线以及 Preview-MPC 参考生成方法进行对比。本文重点回答四个工程问题：EID 整体结构在无扰动低幅双关节运动中是否安全有效；在明确扰动条件下是否改善跟踪与协调误差；Preview-MPC 是否降低参考激励和力矩变化量；以及 (K_o)、(K_u) 参数对稳定性、误差和力矩代价的影响。
>
> 结果显示，EID 整体结构在 P1 无扰动实验中降低髋、膝和协调 RMSE，且未观察到批量运行失败；在 P2-H 髋人工扰动实验中，EID 的全稳态误差低于 PD，但由于人工扰动未记录开始和结束时间，P2-H 不能作为严格扰动窗口结论。P2D 双关节软件力矩扰动给出了可复现扰动窗口：在 ([+6,-4]\ \mathrm{N\cdot m}) 平滑力矩脉冲下，EID 将髋窗口 RMSE 降低 18.2%、协调窗口 RMSE 降低 6.1%，但膝窗口 RMSE 上升 10.0%，且力矩 RMS 与力矩变化量均增加。
>
> Preview-MPC 2 点参考在 P3 中显著降低参考加速度、参考 jerk 和力矩变化量，代价是髋/膝跟踪 RMSE 小幅上升。P4D 参数扫描显示，O0 在软件扰动下无法覆盖完整扰动窗口，O4/O5 均能稳定完成；增大 (K_o) 可降低扰动窗口误差，而增大 (K_u) 几乎不改善窗口 RMSE，只放大 (\eta_u) 活动量。
>
> 本轮结论的边界是：P2D/P4D 支持**输入端等效扰动**下的实物结论，但不能替代真实外部物理接触力实验。下一步应固定 O4+U1 和 O5+U1 两组候选参数，补充可测物理外扰实验，并测试 EID 整体结构与 Preview-MPC 组合后是否能降低力矩变化量。

这个摘要比现在更稳，因为它同时说清楚了：**有效、代价、边界、下一步**。

---

# 三、第 1 节“实验目的”修改

当前 Q2 写成：

> 已补充软件扰动窗口；物理外扰仍需补充

建议改得更明确一些，避免读者误解为“扰动问题已经回答”。

## 替换表格中的 Q2/Q4

| 编号 | 问题                                             | 对应实验     | 本轮回答状态                          |
| -- | ---------------------------------------------- | -------- | ------------------------------- |
| Q1 | EID 整体结构在无扰动低幅髋膝运动中是否安全有效                      | P1       | 已回答                             |
| Q2 | EID 整体结构在扰动下是否降低误差，且不增加协调误差                    | P2-H、P2D | **部分回答：软件输入端等效扰动已回答；真实物理外扰未回答** |
| Q3 | Preview-MPC 是否降低参考加速度、参考 jerk 和力矩变化量，同时不明显破坏跟踪 | P3       | 已回答                             |
| Q4 | (K_o) 与 (K_u) 如何影响 EID 整体结构的稳定性、误差和力矩代价        | P4、P4D   | 已回答本轮无扰动和软件扰动条件；尚不是跨工况最优参数      |

这会让报告从一开始就把边界讲清楚。

---

# 四、第 2 节“控制算法概述”必须改一句关键错误

当前图 1 说明里有一句：

> EID 估计通道根据实测状态与标称预测状态之间的偏差形成输入端补偿

这句话建议改掉。算法文档明确写的是当前更新误差为：

[
\tilde x_k=x_k-\bar x_k
]

而不是 (x_k-\hat x_k)。

## 替换为

> **图 1.** 输入域 EID 整体结构框图。参考生成器提供当前控制周期和下一控制周期的参考位置、参考速度；解析逆模型给出标称前馈力矩 (u_k^*)；EID 估计量经 (K_u) 映射为输入端补偿 (\eta_{u,k})；最终控制律围绕中心反馈状态 (\bar x_k=\hat x_k+\eta_k) 构造。当前实现中，EID 更新使用实测状态与中心反馈状态之间的残差 (x_k-\bar x_k)，而不是实测状态与标称预测状态之间的残差 (x_k-\hat x_k)。

第 2.2 小节里这句话也建议改：

> EID 估计量用于描述真实系统相对标称模型的等效偏差。

改成更稳的版本：

> EID 估计量用于修正标称预测状态，并通过 (K_u) 映射为输入端补偿。本文不把 (\eta_k) 或 (\eta_u) 直接解释为真实外力或真实外部扰动力矩，而是将其作为 EID 整体结构内部的状态修正量和输入补偿量。

---

# 五、第 6 节“结果”改标题，不改数据

数据表基本可以保留，但小节标题需要更准确。

## 6.1 标题建议

现在是：

> P1/P2-H：输入域 EID 的跟踪收益

改成：

> **P1/P2-H：EID 整体结构的稳态跟踪结果**

原因：P2-H 没有扰动窗口，所以不要在标题里暗示“抗扰收益”。你们自己报告也写了 P2-H 不能直接写成严格扰动窗口抗扰结论。

### 6.1 末尾建议加一句

> 因此，P1 可作为无扰动门槛验证；P2-H 只能作为人工扰动工况下的全稳态统计结果，不能作为严格扰动窗口抗扰结论。

---

## 6.2 标题建议

现在是：

> P1/P2-H：输入域 EID 的力矩代价

可以保留，但建议改成：

> **P1/P2-H：误差收益对应的力矩代价**

这一节的重点不是“EID 有代价”这么简单，而是要告诉读者：**力矩变化量是后续必须同步优化的指标**。

### 6.2 结尾替换为

> 本轮结果表明，EID 整体结构的主要代价不是平均力矩 RMS 大幅上升，而是 (\Delta\tau_\mathrm{est}) RMS 上升。后续评价不应只看跟踪 RMSE，还应同步报告 (\tau_\mathrm{est}) RMS、(\Delta\tau_\mathrm{est}) RMS、(\eta_u) RMS 和力矩频谱。

这和你们当前数据一致：P1/P2-H 中 EID 的 (\Delta\tau_\mathrm{est}) RMS 明显上升。

---

## 6.3 P2D 结论建议更硬一点

当前 P2D 写得基本是对的，但建议在表格后面加一个“结论框”。

## 添加文本

> **P2D 小结。**
> P2D 支持的结论是：在当前 ([+6,-4]\ \mathrm{N\cdot m}) 软件输入端等效扰动下，EID 整体结构改善髋关节窗口 RMSE、协调窗口 RMSE 和峰值误差，但没有改善膝窗口 RMSE，并且增加力矩 RMS 与力矩变化量。因此，P2D 不能概括为“EID 所有扰动指标均优于 PD”，更准确的表述是“EID 在输入端等效扰动下表现出部分抗扰收益，同时伴随明确力矩代价”。

这句话非常关键。它会让报告更可信。

---


---

## 6.6 P4D 建议明确参数结论

当前 P4D 很有价值，建议在 6.6 末尾加一个参数建议框。

## 添加文本

> **P4D 参数建议。**
> 本轮不支持继续增大 (K_u)。U1–U4 的窗口 RMSE 几乎重合，而 (\eta_u) RMS 随 (K_u) 增大，因此提高 (K_u) 主要增加补偿量活动，并未带来可辨识的窗口误差收益。下一轮建议固定两组候选参数：**O4+U1 作为主推荐参数，O5+U1 作为备选参数**。O4 的力矩代价较低，O5 的窗口误差略低但力矩 RMS 略高。

---

# 六、第 7 节“讨论”建议压缩

现在讨论部分可以保留，但建议从 4 个小节压缩成 3 个小节：

## 改成

### 7.1 EID 整体结构的有效性与代价

写这几句话即可：

> P1、P2-H 和 P2D 共同表明，EID 整体结构在本轮右髋-右膝台架实验中降低了多项误差指标。P1 支持无扰动门槛验证，P2-H 支持人工扰动工况下的全稳态统计改善，P2D 支持软件输入端等效扰动窗口下的部分抗扰收益。与此同时，EID 的力矩变化量增加也很明确，因此后续必须以“误差收益—力矩代价”共同评价算法，而不能只报告 RMSE。

### 7.2 证据边界

写：

> 本轮证据分为三类。第一类是无扰动稳态跟踪结论，主要来自 P1/P4。第二类是人工扰动全稳态统计结论，来自 P2-H，但由于没有扰动时间戳，不能计算扰动窗口指标。第三类是软件输入端等效扰动结论，来自 P2D/P4D，其扰动时间和幅值可复现，但该扰动不等价于外部物理接触力。因此，本轮尚未完成真实物理外扰抗扰验证。

### 7.3 参数与下一步方向

写：

> 参数结果显示，O0 不满足带扰动可用性；O4/O5 是本轮稳定性和误差表现较好的候选区间。(K_u) 扫描没有显示额外 RMSE 收益，因此下一轮不应继续大范围提高 (K_u)，而应固定 O4+U1 和 O5+U1，进入可测物理外扰验证。同时，Preview-MPC 已显示降低力矩变化量的潜力，应测试其与 EID 整体结构组合后的效果。

这样讨论会更短、更像正式报告。

---

# 七、第 8 节“结论”直接替换

现在结论 6 条是对的，但可以更聚焦。建议替换成下面版本。

## 建议结论

> 本轮实验得到以下结论：
>
> 1. **EID 整体结构通过无扰动门槛验证。** 在 P1 低幅双关节台架实验中，EID 相对 PD 降低髋、膝和协调 RMSE，且未观察到批量运行失败。
>
> 2. **EID 整体结构在软件输入端等效扰动下表现出部分抗扰收益。** 在 P2D ([+6,-4]\ \mathrm{N\cdot m}) 软件力矩扰动窗口中，EID 降低髋窗口 RMSE 18.2%、协调窗口 RMSE 6.1%，但膝窗口 RMSE 上升 10.0%。因此，本轮不能写成“所有扰动指标均改善”。
>
> 3. **EID 的主要代价是力矩活动增加。** P1/P2-H/P2D 均显示 EID 的 (\Delta\tau_\mathrm{est}) RMS 上升；P4D 进一步显示，提高 (K_u) 会放大 (\eta_u) RMS，但几乎不改善窗口 RMSE。

>
> 5. **参数上建议保留 O4+U1 和 O5+U1。** O0 不满足带扰动可用性；O4/O5 均能完成 P4D 完整重复。由于增大 (K_u) 没有带来明确 RMSE 收益，下一轮不建议继续大范围提高 (K_u)。
>
> 6. **本轮结论不等同于真实物理外扰结论。** P2D/P4D 属于输入端等效扰动实验，不能替代外部物理接触力实验。真实物理外扰抗扰能力需在下一轮用可测外力方式验证。

---

# 八、第 9 节“后续工作”改成可执行计划

现在的后续工作方向对，但太像列表。建议改成“下一轮实验计划”，直接给参数、工况、指标。




