# you asked

message time: 2026-06-22 20:07:22

为什么EID方法总有一个更大的误差，请你分析可能的原因？如何通过实验去检测这个误差哪里来的？

---

# chatgpt response

EID 总有更大误差，最常见的原因不是“EID 原理错了”，而是**EID 把原本 PD 闭环里没有的估计误差、相位滞后、噪声、模型残差和补偿力矩都引入了闭环**。在没有明显扰动、扰动很小、或估计器没调好时，EID 反而会把“假的扰动”当成真实扰动去补偿，于是误差比 PD 更大。

你报告里的仿真其实已经出现了这个现象：无扰动 E1 中，PD 的髋/膝误差是 0.0037/0.0040，而输入域 EID 变成 0.0291/0.0244；E3 步态型负载中，输入域 EID 也没有全面优于 PD。报告结论也写得很明确：输入域 EID 是“方向正确但尚未调完”，目前不能直接宣称全面优于 PD 基线。

下面按“原因 → 怎么实验检测”来拆。

---

## 1. 第一类原因：EID 在无扰动时估计出了“假扰动”

输入域 EID 的控制律本质是：

$$
u_k = u_k^* - \eta_{u,k} + K(r_k-\bar{x}_k)
$$

如果 $\eta_{u,k}$ 估计正确，它可以抵消真实扰动；如果 $\eta_{u,k}$ 估计错了，它就会变成一个额外输入，直接把系统推偏。报告里也强调，EID 估计量需要通过观测器增益、补偿增益、滤波和速率限制共同调节，不能只靠增大补偿增益解决。

**检测方法：做 P1 的 EID 影子模式。**

不要一上来让 EID 参与力矩，而是做四组：

| 组别 | 设置 | 目的 |
|---|---|---|
| A | PD，EID 完全关闭 | 基线 |
| B | EID 观测器打开，但 $K_u=0$，不补偿 | 看估计器自己是否产生漂移/噪声 |
| C | EID 观测器打开，记录 $\eta_q,\eta_{\dot q},\eta_u$，但实际力矩仍用 PD | 影子模式，判断 EID 会想补什么 |
| D | EID 真正接入力矩 | 看误差是否由补偿注入导致 |

关键判断：

如果 B/C 中 $\eta_u$ 在无扰动时已经有明显非零均值、周期性波动或高频抖动，说明 EID 把模型误差、参考不一致、噪声或延迟当成了扰动。
如果 C 的预测补偿力矩很大，而 D 一接入误差就变大，说明“误差来自 EID 补偿通道”。

---

## 2. 第二类原因：$\eta_u$ 符号、量纲或关节映射错了

EID 是输入端补偿，符号非常敏感。你现在的软件扰动实验如果写成：

$$
\tau_{\text{sent}} = \tau_{\text{controller}} + \tau_{\text{disturbance}}
$$

而控制律写成：

$$
u = u^* - \eta_u + K(r-\bar{x})
$$

那么理想情况下，若外部等效扰动是 $+\tau_d$，EID 应该估计出同方向的 $\eta_u \approx \tau_d$，然后通过 $-\eta_u$ 抵消它。若符号反了，EID 会把扰动放大成近似 $2\tau_d$。

**检测方法：单关节静态或低频软件力矩偏置测试。**

只开一个关节，轨迹尽量慢或保持小幅正弦。做已知扰动：

$$
\tau_d = +A
$$

然后观察：

$$
\eta_u \approx +A
$$

还是：

$$
\eta_u \approx -A
$$

判断表：

| 现象 | 可能问题 |
|---|---|
| 加 $+\tau_d$，$\eta_u$ 也趋向 $+A$，接入后误差下降 | 符号大概率正确 |
| 加 $+\tau_d$，$\eta_u$ 趋向 $-A$，接入后误差变大 | EID 符号反了 |
| 髋扰动时膝的 $\eta_u$ 变化更大 | joint index 或通道映射可能错 |
| $\eta_u$ 数值比扰动力矩大很多 | $K_u$ 量纲或缩放可能错 |
| $\eta_u$ 很慢才跟上 | $\alpha$ 或 $K_o$ 太小，估计滞后 |
| $\eta_u$ 振荡 | $\alpha$、$K_o$、$K_u$ 或速度通道过大 |

这个实验非常重要，因为它能直接判断“EID 是在抵消扰动，还是在帮扰动”。

---

## 3. 第三类原因：EID 估计有相位滞后，补偿来晚了

EID 观测器不是瞬时知道扰动。它通常类似一阶低通估计：

$$
\eta_{k+1}=\alpha K_o e_k + (1-\alpha)\eta_k
$$

$\alpha$ 小时更平滑，但更慢；$\alpha$ 大时更快，但更容易放大噪声。报告里也指出，$\alpha$ 决定 EID 估计速度，大 $\alpha$ 响应快但噪声大，小 $\alpha$ 平滑但补偿滞后。

如果扰动窗口很短，或者轨迹频率较高，EID 的补偿力矩可能总是落后误差半拍。结果就是：误差已经过去了，补偿才来，反而制造新的误差。

**检测方法：扰动窗口时序对齐。**

对 P2 软件力矩扰动，画同一张图：

```text
tau_disturbance
e_h / e_k
eta_u
tau_sent
```

重点看四个时间：

```text
t_disturbance_start
t_error_peak
t_eta_u_peak
t_recovery
```

计算：

$$
\Delta t_{\eta} = t_{\eta_u,\text{peak}} - t_{\text{error,peak}}
$$

判断：

| 现象 | 解释 |
|---|---|
| $\eta_u$ 峰值明显晚于误差峰值 | EID 补偿滞后 |
| $\eta_u$ 晚到扰动结束后才变大 | $\alpha$ 或 $K_o$ 太小 |
| 增大 $\alpha$ 后误差下降但力矩抖动上升 | 估计速度和噪声之间存在折中 |
| 增大 $K_o$ 后 $\eta_u$ 更快但误差更大 | 可能过补偿或噪声注入 |

---

## 4. 第四类原因：速度测量噪声被 EID 放大成力矩抖动

EID 通常使用位置和速度偏差。速度信号比位置更容易有噪声，尤其是编码器差分速度、通信抖动或低通不足时。报告里明确写到，$K_o$ 太大时会把测量噪声放大到 $\eta$，最终通过 $\eta_u=K_u\eta$ 变成力矩抖动；如果力矩高频抖动明显，应优先降低 $\alpha$、速度通道增益或补偿强度。

**检测方法：频谱 + 通道关断实验。**

做三组：

| 组别 | 设置 |
|---|---|
| Q-only | 只开 `observer_gain_q` 和 `ku_q` |
| DQ-only | 只开 `observer_gain_dq` 和 `ku_dq` |
| Q+DQ | 位置、速度通道都开 |

记录并做频谱：

```text
dq_actual
eta_dq
eta_u
tau_sent
tau_rate
```

判断：

| 现象 | 可能原因 |
|---|---|
| DQ-only 误差更大，力矩变化量明显上升 | 速度通道噪声被放大 |
| Q-only 更平滑但响应慢 | 速度通道有帮助，但需要减小增益/滤波 |
| $\eta_u$ 与 $dq$ 噪声同频 | 速度估计噪声进入 EID |
| PD 中没有同频峰，EID 中出现 | EID 观测/补偿放大噪声 |
| PD 和 EID 都有同频峰 | 可能是传感器、通信或机械结构问题 |

这个实验能定位“误差是不是来自速度通道”。

---

## 5. 第五类原因：EID 破坏了原本已经调好的 PD 闭环

PD 基线是围绕真实测量状态或中心反馈状态调好的。EID 接入后，反馈状态变成 $\bar{x}$，又额外引入 $\eta$ 和 $\eta_u$。如果 $x$、$\bar{x}$、$\eta$ 的相互关系没调好，PD 原本稳定的闭环会被改变。

报告里的控制结构也说明，反馈项是围绕中心反馈状态 $\bar{x}$ 构造，而不是直接围绕测量状态构造；这意味着 $\bar{x}$ 本身的动态会影响反馈误差。

**检测方法：同时画实际误差和中心状态误差。**

记录：

```text
x = [q, dq]
x_bar
r = [q_ref, dq_ref]
eta
```

计算两种误差：

$$
e_{\text{real}} = r - x
$$

$$
e_{\text{bar}} = r - \bar{x}
$$

判断：

| 现象 | 可能原因 |
|---|---|
| $e_{\text{real}}$ 小，但 $e_{\text{bar}}$ 大 | 中心状态偏了，PD 在追错误状态 |
| $e_{\text{bar}}$ 小，但 $e_{\text{real}}$ 大 | EID 内部状态“自洽”，但真实机器人没跟上 |
| $\bar{x}$ 相对 $x$ 有明显延迟 | 观测器动态太慢 |
| $\bar{x}$ 相对 $x$ 有高频抖动 | 观测器噪声太大 |
| EID 接入后 $K(r-\bar{x})$ 比 PD 反馈项更大 | EID 改变了反馈工作点 |

这能判断误差来自“真实系统跟踪差”，还是“中心状态估计差”。

---

## 6. 第六类原因：逆模型前馈与参考轨迹不一致

你当前控制器有解析逆模型前馈 $u^*$。如果参考轨迹的 $q_{\text{ref}}$、$\dot q_{\text{ref}}$、下一步参考 $r_{k+1}$ 不满足离散动力学一致性，逆模型会产生残差。报告里也提到，逆模型权重会影响前馈更偏向一步位置目标还是速度目标；参考轨迹离散一致性差时，这些权重会明显影响前馈力矩和残差。

EID 可能把这个“参考/逆模型残差”当成外扰来估计，于是在每个轨迹拐弯、速度换向、策略点边界产生补偿力矩，导致误差更大。

**检测方法：无扰动下对齐参考边界和 $\eta_u$。**

画：

```text
q_ref
dq_ref
ddq_ref 或估计加速度
u_star
eta_u
tau_sent
tracking error
```

重点看：

| 位置 | 观察 |
|---|---|
| 轨迹换向点 | $\eta_u$ 是否尖峰 |
| 策略周期边界 | $\eta_u$ 是否周期性尖峰 |
| 参考速度不连续处 | 力矩变化量是否变大 |
| 加速度尖峰处 | EID 误差是否同步变大 |

如果 EID 的大误差总是出现在参考导数不连续或换向点附近，说明问题可能不是扰动，而是参考生成/逆模型一致性问题。报告也指出 Preview-MPC 的价值是降低参考高阶激励和力矩变化量，而不是替代 EID 抗扰。

---

## 7. 第七类原因：当前 EID 是逐关节对角补偿，膝髋耦合没有建模

你现在的输入域 EID 主要是逐关节补偿。报告里明确说，当前实现相当于先用对角结构测试 EID；完整膝髋耦合更合理的是非对角 $K_u\in\mathbb{R}^{2\times4}$，让髋、膝位置和速度的 EID 估计共同影响两关节力矩。

所以在真实机器人上可能出现：

```text
髋扰动 → 膝误差变大
膝扰动 → 髋误差变大
```

但逐关节 EID 只在局部关节补偿，无法正确处理交叉耦合，甚至可能把耦合误差误判为本关节扰动，造成更大的协调误差。

**检测方法：P2-H / P2-K 交叉传递实验。**

做两类软件扰动：

```text
P2-H：只给髋关节加 tau_disturbance
P2-K：只给膝关节加 tau_disturbance
```

计算：

$$
\rho_{h\rightarrow k}
=
\frac{\mathrm{RMSE}_k^d}{\max(\mathrm{RMSE}_h^d,\epsilon)}
$$

$$
\rho_{k\rightarrow h}
=
\frac{\mathrm{RMSE}_h^d}{\max(\mathrm{RMSE}_k^d,\epsilon)}
$$

判断：

| 现象 | 解释 |
|---|---|
| 受扰关节误差下降，但非受扰关节误差上升 | 局部补偿有效，但耦合传播变差 |
| 协调误差上升 | EID 破坏了膝髋相对运动 |
| $\rho_{\text{EID}} > \rho_{\text{PD}}$ | EID 放大了交叉传递 |
| $\rho_{\text{EID}} < \rho_{\text{PD}}$ | EID 抑制了交叉传递 |
| 只有某个方向有效 | 髋→膝和膝→髋耦合不对称，需要分别建模 |

---

## 8. 第八类原因：补偿力矩过大，触发限幅或速率限制

EID 误差变大有时不是因为估计错，而是因为估计出来的补偿力矩太大，导致：

```text
tau_sent 被 clip
tau_rate 过大
执行器进入限幅
闭环等效增益变化
```

这会让系统表现得比 PD 更差。报告里也把力矩 RMS、力矩变化量 RMS、最大力矩、限幅次数作为必须检查的指标，因为误差改善不能依赖过大或过粗糙的控制输入。

**检测方法：分开记录限幅前后力矩。**

必须记录：

```text
tau_controller
eta_u
tau_before_limit
tau_sent
saturation_flag
tau_rate
```

判断：

| 现象 | 可能原因 |
|---|---|
| `tau_before_limit` 很大，但 `tau_sent` 被削顶 | EID 补偿触发限幅 |
| 误差峰值出现在限幅后 | 限幅导致补偿失效 |
| 力矩变化量 RMS 很大，但 RMSE 没改善 | 补偿粗糙，代价高收益小 |
| 降低 $K_u$ 后误差反而下降 | 原来是过补偿 |
| 加入 `eta_u` rate limit 后误差下降 | 原来是补偿力矩变化太快 |

---

## 建议你按这个顺序做定位实验

不要一开始就扫很多参数。建议按下面顺序定位：

### Step 1：确认 EID 是否在无扰动时制造误差

```text
PD
EID observer on, Ku = 0
EID observer on, shadow eta_u only
EID full on
```

结论：

```text
如果 shadow eta_u 已经大，问题来自估计器。
如果 shadow eta_u 小，但 full on 后误差大，问题来自 Ku/符号/力矩注入。
```

### Step 2：确认符号和通道映射

单关节、低速、小幅，注入已知软件扰动：

```text
tau_disturbance = +A
tau_disturbance = -A
```

看：

```text
eta_u 是否同方向估计
补偿后误差是否下降
对应关节是否正确
```

### Step 3：确认滞后还是噪声

用同一个扰动，扫：

```text
alpha: 小 → 中 → 大
Ko: 小 → 中 → 大
Ku: 固定小值
```

看：

```text
eta_u 峰值是否晚于误差峰值
eta_u RMS 是否增加
tau_rate RMS 是否增加
RMSE 是否下降
```

### Step 4：拆位置通道和速度通道

做：

```text
q-channel only
dq-channel only
q + dq
```

看哪个通道让误差变大。

### Step 5：检查参考/逆模型残差

无扰动下对齐：

```text
reference boundary
u_star
eta_u
tracking error
tau_rate
```

如果误差总在参考换向、策略边界、速度尖峰处出现，先处理参考平滑或逆模型权重，不要继续加大 EID。

### Step 6：检查膝髋耦合

做 P2-H / P2-K：

```text
髋扰动看膝响应
膝扰动看髋响应
```

计算：

```text
coordination RMSE
cross transfer ratio
S_coord
S_cross
```

如果 EID 只改善受扰关节但放大非受扰关节，问题就是逐关节对角补偿不够。

---

## 一个最小诊断矩阵

你可以先做这个，数据量不大，但定位能力很强。

| 实验 | 设置 | 主要回答 |
|---|---|---|
| D1 | PD，无扰动 | 基线误差是多少 |
| D2 | EID observer on，$K_u=0$，无扰动 | EID 是否估计出假扰动 |
| D3 | EID full on，无扰动 | 补偿是否引入副作用 |
| D4 | PD + 已知软件扰动 | 扰动本身造成多大误差 |
| D5 | EID full + 同一软件扰动 | EID 是否真的抑制扰动 |
| D6 | EID full，但 $K_u$ 取负号或 0.5 倍 | 判断符号/量级是否敏感 |
| D7 | q-only EID | 位置通道贡献 |
| D8 | dq-only EID | 速度通道噪声贡献 |
| D9 | 髋扰动 / 膝扰动分开 | 判断膝髋交叉传递 |
| D10 | 记录参考边界、$u^*$、$\eta_u$ | 判断参考/逆模型残差 |

---

## 最可能的优先级判断

结合你现在的情况，我会优先怀疑这几个：

1. **无扰动时 EID 把模型残差或参考不一致当成扰动**，所以 P1 误差变大。
2. **速度通道噪声或 $K_o/\alpha$ 偏大**，导致 $\eta_u$ 变成力矩抖动。
3. **$K_u$ 量级偏大或符号/通道映射需要确认**，导致补偿不是抵消而是过补偿。
4. **逐关节 EID 没有处理膝髋耦合**，局部误差可能下降，但协调误差或非受扰关节误差变大。
5. **参考导数和逆模型不一致**，EID 在换向点、策略边界或速度尖峰处补偿“假扰动”。

最关键的一句话是：**先让 EID 在不接入力矩的 shadow mode 下“说出它想补什么”，再决定要不要让它真的补。** 如果 shadow mode 下 $\eta_u$ 就已经和误差、噪声、参考边界强相关，那么误差来源基本就在估计器或参考/模型残差；如果 shadow mode 很干净，一接入就坏，那就重点查 $K_u$、符号、限幅和力矩注入路径。